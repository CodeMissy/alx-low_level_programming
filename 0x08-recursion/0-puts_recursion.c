#include "main.h"

/**
 * _puts_recursion - prints a string
 * @s: character used
 * @n: number of times string is printed
 * Return: void
 */

_puts_recursion(char *s)
{
int n;
if (*s <= n)
return(n);
_putchar('\n');
}

