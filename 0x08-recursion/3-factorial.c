#include "main.h"

/**
 * factorial - returns factorial of a given number
 * @n: number of strings
 * Return: factorial
 */

int factorial(int n)
{
if (n == -1)
{
return (-1);
}
else
return (n! = n * (n - 1!));
}
