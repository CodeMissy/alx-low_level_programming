#include "main.h"

/**
 * is_prime_number - checks if num is prime number
 * @n: num
 * Return: 1 if n is a prime number, 0 if n is not a prime number
 */

int is_prime_number(int n)
{
if (n%2 == 0)
return (1);
if (n%2 == 1)
return (0);
}
