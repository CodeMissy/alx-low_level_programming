#include "main.h"

/**
 * rev_string - reverses a string
 * @s: the string reversed
 * Return: reversed string
 */

void rev_string(char *s)
{
for (s = 10; s <= 1; s--)
_putchar(s);
_putchar('\n');
}
