#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if alphabet is uppercase
 * @c: the integer to check
 * Return: 1 if true, 0 if false.
 */

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
