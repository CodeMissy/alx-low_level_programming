#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies and returns sum of a and b 
 * @a: first integer
 * @b: second integer 
 * Return: sum of a and b
 */

int mul(int a, int b)
{
return (a * b);
}
