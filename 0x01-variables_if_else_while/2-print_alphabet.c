#include <stdio.h>
#include <type.c>
/**
 * main - main block
 * Return: 0
 */

int main(void)
{
int ch;
for (ch = 'a' ; ch < = 'z' ; ch++)
}
ch = tolower(ch);
putchar(ch);
{
putchar('\n');
return (0);
}
