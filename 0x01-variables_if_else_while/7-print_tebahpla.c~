#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints lowercase alphabets in reverse
 * Return: 0
 */

int main(void)
{
int ch;
for (ch= 'z'; ch>= 'a'; c--)
putchar(ch);
putchar('\n');
return (0);
} 
