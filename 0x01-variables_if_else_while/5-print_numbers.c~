#include <stdio.h>
#include <stdlib.h>

/*
 * main - prints all single digit base 10 numbers starting from 0
 * Return: 0
 */

int main(void)
{
int i;
for (i=0; i<10; i++)
printf("%i\n", i);
return (0);
} 
