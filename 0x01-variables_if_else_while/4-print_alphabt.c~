#include <stdio.h>
#include <stdlib.h>

/**
 * main - this prints all lowercase alphabets except e and q
 * Return: 0
 */

int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
if (ch!= 'e' && ch!= 'q')
putchar(ch);
putchar('\n');
return (0);
} 
