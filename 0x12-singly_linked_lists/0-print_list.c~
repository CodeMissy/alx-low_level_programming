#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_list - prints all singly list element
 * list_t - singly list
 * @h: head of list
 * Return: void
 */

size_t print_list(const list_t *h)
{
int value;
struct node *next;
}
node *temp = h;

while (temp != NULL)
{
if (temp->next == NULL)
printf("%d", temp->value);
}
else
{
printf("%d, ", temp->value);
}
temp = temp->next;
}
