#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - prints addition of positive numbers
 * @argc: argument counter
 * @argv: argument storage
 * Return: 0 but 1 if there are no digits
 */

int main(int argc, char *argv[])
{
if (argc % 2 = 0)
{
printf("%d\n", atoi(argv[1]) + atoi(argv[2]));
return (0);
}
else
{
printf("Error\n");
return (1);
}
}
