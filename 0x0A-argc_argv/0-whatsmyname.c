#include <stdio.h>
#include "main.h"
/**
 * main - prints program name followed by new line
 * @argc: argument count
 * @argv: arrays containing arguments used in prog
 * Return: 0
 */

int main(int argc __attribute__((unused)), char* argv[])
{
printf("%s\n", argv[0]);
return (0);
}
