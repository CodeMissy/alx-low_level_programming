#include "main.h"
#include <stdio.h>

/**
 * _strcat - contactenates strings src and dest
 * @dest: string one
 * @src: string two
 * Return: *dest
 */

char *_strcat(char *dest, char *src)
{
char *dest, *src;
strcpy(*dest);
strcpy(*src);
strcat(*dest, *src);
_putchar(*dest);
}
