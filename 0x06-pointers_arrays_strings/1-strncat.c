#include "main.h"

/**
 * _strncat - contactenates two strings
 * @dest: string 1
 * @dest: string 2
 * @n: integer used
 * Return: void
 */

char *_strncat(char *dest, char *src, int n)
{
char *dest, *src;
int n;
_strcpy(str1);
_strcpy(str2);
_strncat(str1, str2, sizeof(str1) (strlen(str1) - 1);
_putchar(str1);
}
