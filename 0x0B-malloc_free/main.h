#ifndef MAIN_H
#define MAIN_H

int _putchar(char c);

/* creates an array of chars and initialiazes with a specific char */
char *create_array(unsigned int size, char c);

char *_strdup(char *str);

#endif
